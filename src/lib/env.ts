// This file can be used to manage and validate environment variables.
// Using a library like Zod can help ensure that your environment variables are correctly configured.

/*
import { z } from 'zod';

const envSchema = z.object({
  NEXT_PUBLIC_APP_URL: z.string().url(),
  EXTERNAL_API_BASE_URL: z.string().url(),
});

export const env = envSchema.parse(process.env);
*/
